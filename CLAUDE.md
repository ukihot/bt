# Bakery Text

文字だけのPCゲーム。日本語のみ。画面には線も絵もなく、パン屋の作業ログだけが流れる。
ジャンルは「抽象タワーディフェンス × 謎解き × 和ホラー」。

---

## 1. コア体験

**「このログ、誰が書いてるんだ?」を確かめる体験。テーマは魔除け。**

- プレイヤーは流れる業務ログを検分し、混入する「異常な筆」を見抜いて対処する
- 恐怖のピークは「分かった瞬間」ではなく「**もう遅いと悟った瞬間**」に置く
- 最重要のゾクッは、内容ではなく**筆癖**で異常に気づいてしまう瞬間
  (例:内容は完全に正常なのに、数え方と表記だけが店の癖と違う)
- 目は1つ、画面は3つ。操作できるのは1つだけで、それでも残り2つから目を離せないという構造が、
  恐怖とタワーディフェンスの両方の土台になる

## 2. 絶対原則(和ホラーの文法)

1. **説明しない。** 脅威の正体・理由・由来は最後まで一切開示しない。
   種明かし・どんでん返し・「実は◯◯だった」は禁止。意味が閉じた瞬間に恐怖は死ぬ
2. **距離だけが縮む。** 日を追って外のステータスは不穏になり、売り場のルール変更は激しくなり、
   焼成室の判断はより追い詰められる。そして最後は画面の外(帳場=プレイヤー自身がいる場所)へと寄る
3. **理不尽な作法。** 対処法は民俗的な禁忌として存在するが、理由は永遠に不明
4. **触れることが穢れ。** 誤った接触はプレイヤー自身への「障り」として返る
5. **終わらない。** クリアしてもカタルシスを与えない。除霊完了は存在しない

## 3. ゲームシステム

### 3.1 モニタ(フィールド)
- プレイヤーは帳場に座り、店内3箇所に据えた監視カメラのログを同時に眺めている。画面は上下2段の
  グリッドで、上段(横いっぱい)が**焼成室**、下段左が**外**、下段右が**売り場**の3画面に分かれる
- 3画面は完全に独立して流れる。それぞれが自分の速度でログを生成する
- **操作できるのは焼成室だけ。** カーソル(J/K)と削除・検印(Z/X)はすべて焼成室のログに対して
  のみ作用する。外・売り場に操作対象は存在せず、画面を切り替えるという概念自体がない
- 外・売り場は読むだけの画面だが、無視してよい画面ではない。**売り場は焼成室で「何が正しいか」を
  書き換える発信源**であり(第3.4節)、**外は焼成室を今どれだけ切実に読むべきかを示す体温計**に
  なる(第3.5節)。プレイヤーの注意は「操作している焼成室」と「操作できないが目を離せない外・
  売り場」の間で常に引き裂かれる——「抽象タワーディフェンス」の実体は、この一点集中の操作と
  二点のながら監視の綱引きにある
- ゲームオーバー = 焼成室の侵食が進み、それが伝播して3画面すべてが「別の何かの記録」に
  置き換わること(侵食度は3画面共有の単一値)
- 正常な営業ログが流れる = 収入(店が回る)。侵食が進むほど経済的にも詰む

### 3.2 プレイヤーの動詞
`削除`(Z) / `検印`(X) の2つだけ。カーソル移動は `J`(下) / `K`(上)。すべて焼成室に対して
のみ機能する(画面切り替えの動詞は存在しない)
- 静観は動詞ではない。カーソルの届く範囲を、何も押さずに通過させれば、それが静観になる
- **押した瞬間には何も起きない。** 削除・検印は対象行に「印をつける」だけの操作で、
  実際の判定(合っていたか、間違っていたか)は、その行がカーソルの届く範囲から
  外れる瞬間に確定する。印をつけた直後の安心感と、後から効いてくる結果のズレが
  この作品の時間差の恐怖
- 全ての行にこの同じ2動詞+静観で挑む。何が正しいかは**そのとき有効なルール**(第3.4節)に
  よって変わるが、静観が「安全な既定値」になれる行は「反応してはいけない行」だけ——通常業務行は
  検印、反応すべき行は削除が必要で、そこで何もしなければそれも間違いになる(第3.3節)。間違いは
  悪化を招く
- 外・売り場を眺めている間も焼成室のログは流れ続け、カーソルの届く範囲を勝手に通過して
  確定していく。焼成室から目を離した瞬間こそ危険——見ていなかった行は静観したのと同じ結果になる
- チュートリアルなし。フィードバックはすべてログ内(ダイエジェティック)で返す

### 3.3 反応の三分類(確定仕様)
焼成室のログは、その時点で有効なルールに照らして以下のいずれかに属し、それぞれ**正解の動詞が
ちょうど1つだけ定まる**(2026-07-20 設計変更・実装済み: `domain::log_line::Classification::
correct_action`)。旗揚げゲームの「赤が上がったら白は上げるか上げないか」の緊張感を、常時3種の
判断(検印/削除/静観)の拮抗として持たせるための仕様:

| 分類 | 正解 | それ以外(タタリ) |
|---|---|---|
| 通常業務行 | 検印(何もしないことも含め、それ以外はすべてタタリ) | 侵食度が進み、脅威との距離も縮む |
| 反応すべき行 | 削除 | 侵食度が進み、脅威との距離も縮む |
| 反応してはいけない行 | 静観(検印・削除どちらもタタリ) | 侵食度が進み、脅威との距離も縮む |

- **「タタリ」は分類を問わず一律。** かつては「反応すべき行を見逃しても侵食度が進むだけ」
  「一部の脅威(禁忌集で有効化されたもの)だけ見逃すと距離も縮む」という非対称な例外(旧
  `taboo`フラグ)を設けていたが、これは廃止した(`domain::log_line::resolve`)。分類が変われば
  正解も変わるが、「正解を外せば侵食度も距離も必ず動く」という一点はどの分類でも揺るがない
  ——通常業務行を検印し忘れる/誤って削除することも、もう「安全な誤操作」ではない
- **どの行がどの分類に属すかは固定ではない。** 売り場発のルール変更(第3.4節)によって、それまで
  「反応すべき」だった書き方が「反応してはいけない」に反転する、あるいはその逆が起こる。筆癖
  (第4節)が「異常かどうか」を知らせる一次シグナルであることは変わらないが、「その異常をどう
  扱うべきか」は常に浮動する——ルールが変えるのは分類そのものであって、分類ごとの正解・タタリの
  対応表は変わらない
- 「反応禁止」行は完全ノーヒントにしない。ルールそのものと筆癖に、注意深い者なら事前に読める
  兆候を必ず仕込む
- 初見は引っかかって死んでよい。二周目から背筋を伸ばして読めることが正しい難易度
- ルールが切り替わった直後の数行は、旧ルールへの反射で誤操作しやすい「切り替わりの一瞬」になる。
  これが本作の主要な緊張源のひとつ

### 3.4 ルール変更(売り場)
- 売り場は接客ログの体裁のまま、客の噂話を拾って記録する
  (例:「お客様が「三時の鐘のあとは数えないほうがいいらしいですよ」とおっしゃった」)。
  です・ます調は崩さない——噂話も接客係が書いた応対記録の一部として流れる
- この一文が成立した瞬間から、焼成室の反応ルールがひとつ書き換わる。理由は説明されない
  (第2原則)。有効なルールは重ね書きされていき、矛盾するルールが出た場合は新しいものが優先される
- ルール変更の頻度・複雑さは日を追って増す(第6節)。終盤は一日のうちに何度もルールが上書きされる
- ルール変更そのものにプレイヤーは介入できない。読んで、焼成室での判断を更新するしかない
- **ルールの有効期間は「その日限り」(2026-07-20 実装済み)。** 噂を聞いた瞬間から有効になった
  ルールは、聞いたその日のうちはずっと効き続けるが、翌日の0時(共有クロックが日をまたぐ瞬間、
  第3.7節)に一律リセットされる——「発動から未来永劫」ではなく、店の一日ごとに知識がいったん
  白紙に戻る方が、禁忌の運用として自然。`domain::rules::RuleLedger::reset_day` が `heard` を
  丸ごと消す形で実装している(`Cast` = 誰が何を言うかの割り当ては消えない、噂を聞いた履歴だけが
  消える)
- 2日目以降、0時をまたぐたびに「昨日までの制約が明けた」ことをほのめかす一文を、売り場の
  応対記録として自然な文体で流す(`domain::rule_reset_notice`、焼成室側の `day_marker` と対になる
  形で `screens::playing::spawn::phase_tick` が日替わりのたびにキューへ積む)。「もう気にしなくて
  大丈夫ですよ」のような接客の物言いに留め、「ルールがリセットされました」のようなメタな説明は
  書かない(原則1)

### 3.5 脅威との距離(外)
- 外は定型報告の反復という体裁のまま、**焼成室で今何が起きているかに対する店の外側の反応**を
  示すステータス表示になる。基本は「異常なし」。ルール違反の見逃しや誤操作が重なるほど、この
  定型句が「ゴミが転がってきた」「野良猫が横切った」から少しずつ不穏な報告へと変化していく
- 外にも操作対象はない。プレイヤーは焼成室での自分の判断の結果を、外の報告の変化を通して
  事後的に思い知らされる
- 外の報告が最も不穏な段階に達したときが、原則2(距離だけが縮む)の到達点——脅威は画面の外、
  帳場そのものに達する(第7節)

### 3.6 ローグライク構造
- **ラン間で持ち越すのは知識だけ。** アイテム・強化・実績による緩和は一切なし
- 手続き的なルール開示はしない。ルール変更・脅威それぞれに一貫した「性格」を定義し、法則発見は
  プレイヤーの手探り(「この場合は…こう?」)に委ねる

### 3.7 時刻の流れ(共有クロック)
売り場のルール変更(第3.4節)が「午後に◯◯」のように時間帯を語る以上、「いま店が何時か」が
一意に定まっていなければ判定のしようがない。以下を時刻まわりの構造として確定させる:

- 3画面は同じ店を同時刻に映す別々の定点観測である以上、**「いま何時か」は店にひとつしかない。**
  各画面はこの単一の時刻(共有クロック)を、自分の書き手の癖に合わせて書き写しているだけで、
  画面ごとに勝手な時刻を持ち出してはならない
- 共有クロックはフェーズ(仕込み/開店/ピーク/閉店/夜)の順に沿って一方向にのみ進む。各フェーズは
  対応する時間帯を、そのフェーズの持続時間いっぱいに使って進行する——同じフェーズ内でも時刻は
  ランダムに前後せず、常に直前より進んだ値になる
- 各画面が実際にログへ書き出す頻度(スポーン間隔)は画面ごとに異なる(売り場は密、外は疎)が、
  これは「クロックを覗く頻度」の違いであって、「クロックそのものが画面ごとに違う」わけではない
- **焼成室の偶数分ルール(第4節)は、この共有クロックに対する焼成室特有の書き癖(丸め)である。**
  真の時刻が奇数分であっても、焼成室の通常行は偶数分に丸めて記帳される。奇数分がそのまま出た
  場合、それ自体が異常のサイン(第4節の逸脱系)であり、クロックの不具合ではない
- **客の噂話が時間帯を語ったとき、それは共有クロックの現在値から一意に判定できる「時間帯」を
  指す。** 時間帯の呼び名(朝・昼・午後・夕方・夜など、客が実際に使う言葉)と、内部のフェーズ/
  時刻範囲との対応表はひとつだけ持ち、噂話の生成側と判定側(焼成室)の双方が同じ表を参照する。
  生成側と判定側で別々の対応表を持たない——でないと「午後と言われたのに焼成室ではまだ午後と
  判定されない」というズレが生まれ、原則1(説明しない)が意図しない形で崩れる
- 具体的な時間帯の区切り(午後は何時から何時までか等)や、フェーズ内での時刻の進み方(線形か、
  フェーズ境界で速度が変わるか)は数値調整の対象として第9節に残す。ここで確定させるのは構造
  (単一の共有クロック・一方向にのみ進行・書き手ごとの丸めは表示層の話・時間帯対応表はひとつだけ)
  であって、具体的な数値ではない

## 4. 筆癖システム

正常ログには一貫した筆癖を仕込み、プレイヤーを無意識にその文体に浸らせる。3画面はそれぞれ
立場の異なる書き手(職人の作業記録/警備の定点観測記録/接客係の応対記録)を想定し、文体そのものを
画面ごとに分ける。操作・判定が生じるのは焼成室だけだが、外・売り場の文体もそれぞれの役割
(距離のステータス/ルールの発信源)に沿って作り込む。

**焼成室**(職人の作業記録・常体・技術寄り。実装時に確定させ全文で厳守。プレイヤーが実際に判断し
操作する唯一の画面):
- バゲットは「本」で数える(個ではない)
- 時刻は必ず偶数分
- 「焼き上がり」であって「焼きあがり」とは書かない
- 文末に句点なし
- 主な異常:表記の乱れ・数え違い・記載漏れ。ここに**現在有効なルール**(第3.4節)が重なり、
  同じ書き方が日によって「反応すべき」にも「反応してはいけない」にもなる
- 3異常はどれも常に「反応すべき行」(正解は削除)で、見逃す/誤操作すればどれも等しくタタリになる
  (第3.3節、2026-07-20 実装済み)。禁忌集の該当噂(「焼き上がりの字だけは崩さないでほしい」)は
  この事実を客の口から予告するだけの伏線であって、噂を聞いた/聞いていないで扱いが変わる特別な
  異常ではない——分類ごとの正解・タタリの対応は第3.3節の1枚の表に統一されている
- **ルール・禁忌の類は一切書かれない。** 「これが今のルールだ」と読めてしまう文面が焼成室に
  混ざると、プレイヤーの操作対象であるこの画面でルールが宣言・確定してしまったかのように見える
  ——それを避けるため、ルールに関する文面の出所は売り場一箇所だけに絞る(第5節)

**外**(警備の定点観測記録・無機質・報告体。読むだけの画面):
- 「異常なし」を基本単位とする、短い定型報告の反復
- 例:「異常なし」「ゴミが転がってきた」「野良猫が横切った」
- 定型報告の文法を保ったまま、語彙だけが徐々に不穏な方向へ動く(第3.5節)。プレイヤーはここに
  触れない——触れる対象がそもそも存在しない

**売り場**(接客係の応対記録・敬体・口語寄り。読むだけの画面):
- 客への発話・応対は「です・ます」調で統一する(焼成室の常体とは混ぜない)
- 例:「いらっしゃいませ」「かしこまりました」「◯◯円のお返しです」
- 通常の応対記録に紛れて、客の噂話が同じ敬体のまま流れ、そのつど焼成室のルールを書き換える
  (第3.4節)
- **ルールに関する文面はすべてここに集約する。** 今まさに効いているルール変更(第3.4節)も、
  店に古くから伝わる禁忌集(第5節)も、区別なく同じ客の噂話・世間話として流れる——他の2画面は
  ルールに関する文面を一切持たない。売り場の情報量が他の2画面より多いのはそのためで、意図的な偏り

**異常は内容より先に筆癖でバレる。** 脅威図鑑は「何をするか」ではなく「どう書くか」で差別化する。
敵の見た目が存在しない以上、**文体=敵デザイン**である。

## 5. 禁忌集(売り場の噂話)

売り場の客の噂話・世間話に紛れて流れる何気ない一言が、実は攻略情報かつ禁忌集(第4節)。**焼成室
には一切書かれない**——店主のメモという体裁はもう取らない。読むだけの画面(売り場)だけがルールの
出所であることを徹底し、操作対象の画面(焼成室)でルールが宣言・確定したかのように見える事故を
構造的に防ぐ(第3.4節・第4節)

- 店に古くから伝わる言い伝えも、今まさに効いているルール変更(第3.4節)も、同じ客の噂話として
  同列に流れる。両者が矛盾した場合は、あとから流れてきた噂のほうが優先される——理由は語られない
- 例(いずれも客の噂話として):「奇数分に帳面をつけると良くないらしい」「夜の納品だけは断った
  ほうがいい」「三のつく日は窯の火を落とさないほうがいい」「同じ話を二度されても続けて聞かない
  ほうがいい」等
- 理由は絶対に書かない。守れば静か、破れば来る
- 禁忌集の大半は「避ければ静か、破れば来る」型の回避系だが、民俗的なお守りの体裁で「正しく
  対処すると、通常より深く救われる」型の援助系も1種類混ざっている(実装は
  `domain::rumors::Effect::Relieve`)。原則2(距離だけが縮む)に配慮し、援助系はcorruption
  (プレイヤーには数値を見せない侵食度)だけを通常より多く回復させ、`Zone`(脅威の物理的な距離)
  そのものは一切動かさない——脅威が実際に後退することはなく、「今回はいつもより深く救われた」と
  いう手応えだけが返る
- 禁忌集の項目リスト = 実質のレベルデザイン。日ごとに一項ずつ意味を持ち始める構成
- **禁忌同士の絡み合い(「◯◯の噂をした人物は嘘つきだ」型)は、方向性の検討のみに戻した
  (2026-07-21)。** 唯一の実装例だった `Discredit`(項目9→項目7)は、対象にしていた裏口
  (禁忌#8)ごと削除した(第9節「操作対象の範囲」参照。外・売り場が操作対象でなくなったことで、
  裏口も含め`Outside`/`Floor`固有の反応型脅威が丸ごと不要になったため)。カタログに実例がない
  まま`Effect::Discredit`だけをコードに残すのは中身のない分岐でしかなかったので、機構ごと削除
  した——`RuleLedger::verdict`/`relief_bonus`はもう話者の信用を確認しない、`Cast::roll`も
  discredit避けの再抽選をしない。今後また入れ子構造を実装するときは、その時点で実在する禁忌に
  合わせて機構ごと作り直す
- 終盤に示してよい事実はただ一つ:**客が語った最後の噂の中身が、プレイヤーの入店より後に起きた
  出来事を指している**こと。それ以上は何も語らない

## 6. 一日の構成(1ウェーブ)

- 仕込み(深夜〜早朝):焼成室・売り場とも静か。禁忌集や筆癖を「読む」時間。外の「異常なし」も
  まだ揺るがない
- 開店〜午前:焼成室の通常業務行が主。経済が回る。軽度の異常が混ざり始める。売り場のルール変更も
  まだ穏やか
- ピーク:売り場の流速が最大になり、ルール変更の頻度も上がる。焼成室も負荷が高い。外は変わらず
  静かなぶん、油断が生まれる
- 閉店〜夜:焼成室の流速が落ち、売り場のルール変更は減るが、外の報告が不穏な方向へ動き始める。
  夜は外の変化に気づきにくい時間帯
- 日をまたぐと、外の報告の不穏度の基準線が一段上がり、売り場のルール変更もより頻繁・複雑になる
  (原則2)。焼成室に集中するほど、ルールの書き換えと距離の変化から目が離れていく——その皮肉
  自体が終盤の難易度になる

## 7. 演出の到達点(順序固定)

1. 序盤:筆癖の違和感(「読めてしまった」ゾクッ)。画面ごとに異なる文体に馴染むほど、逸脱が見えてくる
2. 中盤:誤操作への障り — 業務ログの文法のまま二人称が現れる。操作の原因になった行と同じく
   焼成室に紛れ込む
   `06:52 あなたは 行を 削除した`
3. 終盤:**呼ばれる。** プレイヤーが開始時に入力した名で一度だけ。ただしこの一行は焼成室・外・売り場
   のどの画面にも属さない——グリッドの外、画面と画面の隙間に直接差し込まれる。監視カメラ越しに
   見ているだけのはずが、初めて画面の外から名指しされる
   `07:14 ◯◯さん、裏口`
   この行にはいかなる操作もしてはいけない。削除も検印も「返事」である
4. 最終:守り切っても終了ログは出ない。閉店記帳の後、翌朝の仕込みログが3画面とも普通に始まり、
   静かに流れ続ける。日課は引き継がれた

## 8. 禁止事項(実装・テキスト生成時)

- グラフィック・罫線・アイコン・色による強調(文字のみ。強調も文体で行う)
  (例外1(2026-07-21拡張):CRT的な環境ノイズとしての一文字アーティファクトは、FG/DIMの2色と
  既存3書体の範囲内に限り許可。ノイズ字形への一瞬の差し替えに加え、FG/BGを入れ替える反転表示・
  1pxの縁取りの2種も同じ条件下で許可する——いずれも特定の行の意味に紐付けず、画面全体から
  ランダムに選んだ1文字だけに、短時間だけ作用する環境演出としてのみ使う。詳細は第10節
  `src/screens/playing/glitch.rs`(`glitch_flicker`)。
  例外2:日数インジケータ(2026-07-20追加)は、7セグメント表示を模した小さな計器として、
  FG/DIM 2色の矩形(`BackgroundColor`)7つ×4桁で実装している——文字ではなく矩形そのものを
  使う点でこの原則の狭い例外だが、色は2色のまま・特定の行の意味とも無関係な常時表示の計器
  である点はCRTグリッチと同じ位置づけ。詳細は第10節 `src/screens/playing/day_indicator.rs`。
  例外3(2026-07-21追加、同日中に常時アニメーション版から静的焼き込み版へ作り直し):表示中の
  行は1文字ごとに独立したセルとして描画しており(第10節 `LineCells`)、これを使って各行の
  スポーン時に1文字ごと低確率でFGの代わりにDIMを焼き込む、ごく薄い「インクのムラ」を敷いて
  いる。最初は経過時間で明滅させる常時アニメーション版を試したが、全画面・全行が同じ波形で
  同時に切り替わるため「統一感がありすぎる」「チカチカする」と分かり、以後は一切アニメー
  ションさせない静的な焼き込みに変えた——行ごとに新しく引く乱数で決まるため行同士が同じ
  模様にはならず、二度と動かないのでちらつきが生じようもない。詳細は第10節
  `src/screens/playing/spawn.rs`(`spawn_line_ui`))
- 脅威の正体の説明、ネーミングによる正体の示唆(図鑑名は現象の記述に留める)
- ジャンプスケア的な演出(音・点滅)。恐怖は常に「読み」から生まれること
- チュートリアル、目標表示、正解/不正解の明示的なUI通知
- 勝利演出、エンディングの解決感
- 派手な文字化け。異常は「フォーマットは正しいのに一語だけ違う」を主軸に

## 9. 未確定(今後の検討)

- 禁忌集の全項目リストと解放順
- 脅威図鑑:「どう書くか」ベースの書き分け一覧
- 経済パラメータ(売上と侵食率のバランス)
- 売り場発ルール変更の具体的な文法・語彙集(噂話のバリエーション、同時に有効なルール数の上限)
- 外のステータス表示のエスカレーション段階は`domain::generate::outside_body_pool`
  (2026-07-20)で第一版を実装した——corruption(0-100、プレイヤーには数値を見せない)を4段階
  (calm<25/uneasy<50/alarmed<75/dread)に区切り、段階ごとに専用の語彙プールを持たせている。
  「異常なし」はどの段階でも完全には消えない(出現数を減らすだけ)よう意図的に残してあるが、
  閾値の具体的な数値と各段階の語彙そのものは第一稿で、CLAUDE.mdが本来求める「具体的な語彙・段数」
  の検討はまだ済んでいない
- 焼成室における筆癖(第4節)と有効ルール(第3.4節)の重ね合わせ判定ロジックは
  `domain::rules::RuleLedger`(2026-07-20リファクタ)で一般化済み――噂を「発話順の台帳」として
  記録し、`verdict()`が新しい順に走査して最初に一致した`Enable`/`Void`を採用する形で、
  「新しいものが優先される」をペアごとのハードコードなしに実現している。ただし`Condition`は
  現状`DayHasThree`1種のみ、`Effect`も`Enable`/`Void`/`Relieve`の3種のみで(`Discredit`は
  唯一の実例ごと2026-07-21に削除——下記「操作対象の範囲」参照)、禁忌集の項目数が今後大きく
  増えたときにこの3種で表現しきれるかは未検証のまま
- 禁忌集の噂に紐づく客(`domain::customer::Customer`)のトーン・語調変化(客ごとに応対の言い回しを
  変える等)。名前を持たせるところまでは実装済みだが、客ごとの個性づけそのものは今後の検討
- 客の噂話が使う時間帯語彙(朝・午後・夕方等)と、共有クロック(第3.7節、実装は`domain::clock`)の
  時刻・`Phase::hour_range` との対応表の具体的な区切り
- 仮題の正式決定
- **検印(Stamp)・タタリの統一(第3.3節、2026-07-20 実装済み)。** 分類ごとに正解の動詞が
  ちょうど1つ定まる形(`Classification::correct_action`: 通常業務行=検印/反応すべき行=削除/
  反応してはいけない行=静観)に作り直し、検印が一度も「正解」にならないという欠落を、新しい
  `ThreatKind`を増やすことなく解消した。同時に`taboo`フラグ・`Effect::Enable`の`taboo`パラメータ
  ・`Verdict::Active`の`taboo`ペイロードはすべて廃止——「反応すべき行を見逃しても侵食度だけ」
  という非対称な例外がなくなったことで不要になった(`domain::log_line::resolve`,
  `domain::rules::Verdict`)。表記の乱れ(「焼きあがり」)を噂を聞いた日だけtaboo化する案は、
  この統一によって不要になったため見送った——3基本異常はもともと常に「反応すべき行」であり、
  この表に最初から乗っている
- **禁忌同士の絡み合い(第5節)は方向性の検討のみで、実装は0件に戻った(2026-07-21)。**
  唯一の実例だった`Effect::Discredit`(項目9→項目7)は、対象にしていた裏口(禁忌#8)ごと
  削除した——詳細は下記「操作対象の範囲」の項
- **ルールの日次リセット(第3.4節、2026-07-20実装済み)。** `RuleLedger::reset_day`が`heard`を
  丸ごとクリアする形で実装した——「昨日の噂は今日には効かない」という設計判断
- **操作対象の範囲(第3.1〜3.2節、2026-07-17再設計→2026-07-21実装追従済み)。** 第3〜5節は
  2026-07-17に「焼成室=唯一の操作対象/売り場=ルール変更源/外=脅威距離ステータス」という
  パネル役割の再設計を反映して書き換えていたが、実装(`screens::playing::{pane,input,render,
  setup}.rs`)はしばらくの間その前のアーキテクチャ(`ActivePane`によるH/L切り替え、外・売り場
  にも独自のカーソル窓)のままだった。2026-07-21、実装をこの再設計に合わせて作り直した:
  `H`/`L`キーとその入力ハンドラ・`ActivePane`リソース・パネル見出しの選択表示を丸ごと削除し、
  `J`/`K`/`Z`/`X`は常に焼成室の`Pending`だけを操作する形に固定した(第10節参照)。あわせて
  `screens::playing::spawn::line_spawn`は、外・売り場の行が`Pending`から外れる際に
  `domain::resolve()`を一切呼ばないよう変更した——この2画面はもう操作対象ではないため、
  「正解」の概念自体が存在せず、呼んでしまうと`mark`が常に`None`であることそのものが
  タタリ扱いされてしまう(第3.3節の対応表はどの分類でも「正解の動詞が1つ」を前提にしており、
  操作不能な画面はそもそもその前提の外にある)。ドメイン側も追従し、`Outside`/`Floor`固有
  だった反応型脅威(禁忌#7閉店時間・禁忌#8裏口・禁忌#9外の異常なし、`ThreatKind::ClosingTime`/
  `BackDoor`/`OutsideRepeat`)と、それらを有効化していた禁忌集の該当3項目、および禁忌#8を
  対象にしていた`Effect::Discredit`(禁忌同士の絡み合いの唯一の実例)を削除した——操作不能な
  画面向けの「反応すべき/してはいけない」コンテンツは、正解を確かめる手段がそもそもないため
  維持する意味がなかった。共有クロック(`domain::clock::DayClock`、第3.7節)は元から実装済み

## 10. 実装状況(Bevy 0.19 / `src/`)

Bevy 0.19 で縦スライスを実装済み。0.19 は Text が Parley ベースに刷新されており、
`TextFont.font_size` は `FontSize::Px(_)` を、`font` は `FontSource`(`.into()`変換)を要求する点、
`Timer::finished()` が `is_finished()` に改名された点、`EditableText::value()` が `SplitString`
(`.to_string()` で `String` 化)を返す点が旧バージョンからの主な差分。

`src/` はドメインロジック(`domain/`)と ECS 層(`app_state.rs`/`game_data.rs`/`screens/`)を分離する
構成にしている。モジュールファイルは Rust 2018+ 方式(`foldername.rs` + `foldername/`)を使い、
`mod.rs` は置かない。

- `src/domain/`: ゲームルールの純粋ロジック。`Component`/`Resource`/systems を持たず、`Verb`/`Pane`/
  `Zone`/`Phase`/`LogLine`/`resolve()` などのキー入力すら知らない型・関数のみで構成しているため、
  `App` を起動せず `cargo test` だけで単体テストできる
  - `verb.rs`: `Verb`(`Delete`=削除/Z、`Stamp`=検印/X の2つのみ。静観は `Option<Verb>` の `None` として
    表現し、専用のバリアントは持たない)。キー入力からのマッピング(`KeyCode` → `Verb`)は ECS 層の
    `screens/playing/input.rs` 側に置き、ドメイン層を Bevy の入力型から独立させている
  - `pane.rs`: `Pane`(`Kiln`=焼成室/`Outside`=外/`Floor`=売り場)。各画面の表示名・カーソル窓の行数
    (`capacity`)・自分のフェーズ乗数を掛けた抽選間隔(`spawn_interval_secs`)・`Zone` の現在の
    段階がその画面を指しているか(`matches_zone`)を持つ。`capacity` は `Kiln` を基準に黄金比 φ で
    `Outside`/`Floor` を導出しており(8:5)、`screens/playing/setup.rs` の各画面ボックスの `flex_grow`
    (画面上の縦の面積比)もこの 8:5 に揃えてある
  - `zone.rs`: `Zone`(外周→内側→帳場)。`Pane::matches_zone` を介して原則2(距離だけが縮む)の
    「どの画面が狙われやすいか」を駆動する
  - `clock.rs`(第3.7節): `DayClock`。24時間が実時間4分(`GAME_MINUTES_PER_REAL_SECOND = 6.0`)で
    一方向にのみ進む、店にひとつだけの共有クロック。`advance(real_seconds) -> wraps` が経過した
    実時間ぶん進めて日をまたいだ回数を返す(ほぼ常に0、稀に1)。`hour()`/`minute()`/
    `minute_is_odd()` のみを公開し、時刻を独立した乱数で再抽選する経路を持たない。`#[cfg(test)]`
    に「日内では時刻が絶対に後退しない」「実時間4分で24時間ぶん進む」の単体テストあり
  - `phase.rs`: `Phase`(仕込み/開店/ピーク/閉店/夜、フェーズごとに行の流速が変化)。`hour_range()`が
    24時間を隙間なく分割しており(仕込み0-7時/開店7-11時/ピーク11-14時/閉店14-19時/夜19-24時)、
    `for_hour(hour)` がこの分割を唯一の情報源として `DayClock` の現在時刻からフェーズを逆算する
    ——フェーズはもう自前のタイマーを持たず、常にクロックの関数として導出される
  - `timestamp.rs`: `even_minute_of(clock)` が共有クロックの真の分を焼成室の書き癖(偶数分への
    丸め、第3.7節)に変換する。時刻・本数の文字列生成に使う共通ヘルパーもここに置く
  - `log_line.rs`: `LogLine`/`Classification`/`LineFont` と、判定結果を返す `resolve()`
    (2026-07-20、`taboo`フラグ廃止・`Classification::correct_action`導入のリファクタ後)。
    `Classification::correct_action()` が分類ごとの唯一の正解(`Normal`=検印/`ShouldReact`=削除/
    `ShouldNotReact`=`None`=静観)を返す純粋関数で、`LogLine`はもう`correct_verb`を自前で持たない。
    `resolve(classification, action: Option<Verb>, relief: f32)` は `action == classification.
    correct_action()` かどうかだけで分岐し、一致しなければ分類を問わず一律に「タタリ」
    (corruption増加 + `zone_bump: true`)を返す——かつて存在した「反応すべき行を見逃しても
    corruptionだけ」という非対称な例外はない。`relief`(`LogLine::relief`、援助系の禁忌用)は
    正解を引き当てたときの corruption 回復を上乗せするだけで、`zone_bump`には影響しない。
    `#[cfg(test)]` に単体テストあり
  - `customer.rs`: 「お客様」を昇格させた軽量エンティティ。`Customer`(今のところ`name`のみ)と、
    `rumors::CATALOG` から参照される`CustomerId`(`CUSTOMERS`配列への添字)。噂の話者を特定できる
    ことが目的で、客ごとのトーン変化などの個性づけはまだ乗せていない(第9節)
  - `rules.rs`: 禁忌集の**解決**を担う、カードゲームの継続的効果解決に近い汎用エンジン。`RuleLedger`は
    「今日実際に語られた噂」を`Vec<RumorId>`として発話順に記録するだけの台帳——ルールの
    上書き・矛盾解決はすべて`verdict(pane, threat, ctx) -> Verdict`が担う。`verdict`は台帳を
    **新しい順**に走査し、`threat`に言及する最初の`Enable`/`Void`を採用する——第3.4節「矛盾する
    ルールが出た場合は新しいものが優先される」を、噂の種類が増えてもペアごとの追加コードなしに
    満たす。一致が無ければ`ThreatKind::Repeat`だけ`Active`(焼成室の反復は噂を聞く前から常時
    反応対象)、それ以外は`Suppressed`が既定値。`Verdict`は`taboo`ペイロードを持たない単純な2値
    (`Active`/`Suppressed`)——分類ごとの正解を外せば必ずタタリになる(第3.3節)ので、個別の
    重み付けはもう不要。`relief_bonus(threat)`は同じ新しい順スキャンで`Effect::Relieve`を探す
    姉妹クエリ(援助系、下記`rumors.rs`参照)。`reset_day()`(第3.4節の日次リセット)は`heard`を
    丸ごとクリアするだけの操作——聞いた効果はすべてその日限りで消える。話者そのものは`Cast`
    (`RuleLedger`が`new()`で1回だけ`rand::rng()`から生成し、以後ランの間ずっと固定。
    `reset_day()`後も再ロールされない)が握る——`CATALOG`の各項目に誰が対応するかをランごとに
    ランダムに割り当てることで、「同じ噂は毎回同じ客が言う」という覚えるだけのパターンに
    ならないようにしている(ローグライク原則、第3.6節)。`#[cfg(test)]`に単体テストあり(`Enable`
    後に`Active`になること、`Void`が`Condition`成立時のみ効くこと、`reset_day()`が`heard`だけ
    消して`Cast`は残すこと、`Cast`の割り当てがランごとに変わること、ほか)。誰かの信用を失わせる
    `Effect::Discredit`とその解決ロジック(`is_discredited`)は2026-07-21に削除した——唯一の
    実例(項目9→項目7)が対象にしていた裏口の噂ごと消えたため、実例のない機構だけを残す理由が
    なくなった(下記`rumors.rs`、および第9節「操作対象の範囲」参照)
  - `rumors.rs`: 禁忌集(第5節)の**データ**。`ThreatKind`(`Repeat`/`NightDelivery`)が「どの
    異常パターンか」を、`Effect`(`None`=伏線のみ/`Enable{pane, threat}`/`Void{threat,
    condition}`/`Relieve{threat,bonus}`=援助系)が「聞いた瞬間に何が起こるか」を1箇所に集約する。
    どちらも焼成室(`Pane::Kiln`)だけが対象——`Outside`/`Floor`固有だった`ThreatKind`
    (`OutsideRepeat`/`ClosingTime`/`BackDoor`)と、それらを有効化していた禁忌集の3項目
    (禁忌#7閉店時間・#8裏口・#9外の異常なし)、および#8を対象にしていた`Effect::Discredit`
    (禁忌同士の絡み合いの唯一の実例)は2026-07-21に削除した——外・売り場はもう操作対象では
    ないため(第3.2節)、「反応すべき/してはいけない」コンテンツを維持する意味がなくなった
    (第9節「操作対象の範囲」参照)。`CATALOG: &[RumorDef]`が禁忌集の全7項目の本文(`{name}`
    プレースホルダを`RuleLedger::speaker_of`の名前で埋める、`timestamp::fill_name`参照)・`Effect`
    を保持する唯一の情報源——新しい禁忌を1つ足すのに必要なのは基本ここへの1行だけ。
    `rumor_line(clock, ledger, rng)`がランダムに1件選び`ledger.hear(id)`で記録し、他の売り場行と
    同じく時刻を付けて文面を返す。`#[cfg(test)]`に単体テストあり
  - `threats.rs`: 禁忌集の判定を一切持たない、純粋なコンテンツ生成だけの層。`deviation_threat_line()`
    (焼成室の主異常: 表記の乱れ・数え違い・記載漏れ。常に`ShouldReact`/`削除`、`rules.rs`と一切
    関わらない)、`night_delivery_threat_line()`(禁忌#2、焼成室固有、時刻+`{n}`入りテンプレート、
    常に`ShouldNotReact`)。かつてあった`back_door_threat_line()`/`closing_time_threat_line()`
    (禁忌#8・#7)は2026-07-21に削除済み(下記`generate.rs`参照)。`#[cfg(test)]`に単体テストあり
  - `generate.rs`: 画面ごとの通常業務行(焼成室=技術記録/外=定点報告/売り場=接客応対、第4節)、禁忌集の
    噂話(売り場限定、第5節)、呼びかけ(反応禁止・奇数分がサイン)、演出の到達点(二人称の障り・
    呼ばれる)の生成ロジック。`generate(pane, clock, zone, day, last_normal, ledger, corruption, rng)`
    は`Phase::for_hour(clock.hour())` でフェーズを導出したうえで `Weights`(通常/噂話/表記逸脱/反復/
    `taboo_event`/呼びかけの6バケット)を画面ごとに計算して抽選する。`corruption`(第9節: 外の
    エスカレーション用、下記)以外の用途にはまだ使っていない。`deviation`/`repeat`/`taboo_event`
    は焼成室(`Pane::Kiln`)だけが非0の重みを持つ(2026-07-21、下記参照)——`Outside`/`Floor`は
    `weights_for`の時点でこれら3バケットが常に0なので、`generate()`側に画面ごとの分岐を作らずに
    「焼成室以外では抽選そのものが起こらない」を保証している。`taboo_event`(夜の納品、禁忌#2)は
    `ledger.verdict()`で`Active`と判定されるまで重み0で、そもそも起こらない(起きるが無害、では
    ない)。`repeat`バケットは三のつく日の禁忌(#3)で無効化され得る焼成室の反復——
    `ledger.verdict(pane, ThreatKind::Repeat, ...)`と`ledger.relief_bonus(ThreatKind::Repeat)`を
    引いたうえで`repeated_line()`が`Classification`/`relief`付きの行に組み立てる。噂話バケット
    (`rumor`)は焼成室・外では常に0 ——
    禁忌集を「店主のメモ」として焼成室に混ぜていた旧実装をやめ、ルールに関する文面の出所を売り場一箇所
    (`rumors::CATALOG`)だけに絞ってある(第4・5節)。呼びかけバケットは `clock.minute_is_odd()` が
    偽の間は重み0になる——「奇数分がサイン」という第4節の性質は、行を生成してから奇数分を後付けする
    のではなく、そもそも偶数分には呼びかけが選ばれないという形で保証している(`call_line` 冒頭の
    `debug_assert` 参照)。唯一の例外は `corrupted_line`(第7節の最終到達点、「別の何かの記録」)で、
    これは意図的に共有クロックを読まず時刻を独立乱数で決める——時間そのものが意味を失ったことの表現で
    あり、バグではない。焼成室だけが`deviation`/`repeat`/`taboo_event`という反応型バケットを持つのは
    専用コードパスではなく、`weights_for`が他の2画面ではそれらを常に0にしているだけ(上記参照)。
    外画面の通常行だけは`corruption`(0-100、プレイヤーには数値を一切見せない、第8節)に応じて
    `outside_body_pool`が`OUTSIDE_CALM`/`UNEASY`/`ALARMED`/`DREAD`の4段階から語彙プールを切り替える
    (第3.5節、第9節)——「異常なし」はどの段階でも完全には手放さない(出現数を減らすだけ)
- `src/app_state.rs`: `AppState`(Title/Playing/Lost)のみ
- `src/game_data.rs`: `GameData` リソース(日数・フェーズ・共有クロック(`domain::DayClock`)・
  `Zone`・侵食度・売上・禁忌集台帳(`domain::RuleLedger`))。`phase` は独自タイマーを持たず、`clock` から毎tick
  再導出される値のキャッシュでしかない(`screens::playing::spawn::phase_tick` 参照)ので、
  両者が食い違うことは構造上ありえない。各画面固有の状態(スポーンタイマー・直前の通常行・台本待ち行)
  は `screens::playing::pane::PaneRuntime` へ移したため、ここに残るのは3画面がすべて参照する本当に
  グローバルな値と、呼ばれる用の `pending_intrusion: Option<LogLine>` のみ
- `src/screens/`: `AppState` の画面ごとに1モジュール、各々が自己完結した `Plugin`
  - `title.rs`: 名前入力(`EditableText`)→ Enter で開店。入力欄自体は画面外へ絶対配置して不可視化し、
    実際に見えているのは7セル固定幅の表示専用グリッド(`NameCell`)——`sync_name_cells` が毎フレーム
    `EditableText::value()` から1文字ずつミラーする。半角・全角が混在してもセル幅が文字幅に左右されない
    ので、下線とのズレが原理的に起きない。下線はセルの外枠ではなく各セルの直下に「―」を1本ずつ置く
    形で表現する(第8節準拠、罫線は使わない)
  - `lost.rs`: ログが「別の何かの記録」に置き換わる演出を数秒流し、無言で `AppState::Title` に戻る
    (勝敗UIなし)
  - `playing/`: ログ画面本体。1ファイルへの肥大化を避けるため関心ごとに分割している
    - `pane.rs`: `PaneRuntime`(その画面の `spawn_timer`・`last_normal_line`・`pending_scripted` を
      持つ `Component`。画面ごとの `Entity` に1つ)のみ。かつてここにあった `ActivePane`
      (`J`/`K`/削除/検印 の宛先を指す `Resource`、`H`/`L` で切り替え)は2026-07-21に削除した——
      宛先は常に焼成室で固定なので、切り替え対象を指し示すための状態そのものが不要になった
    - `setup.rs`: `PlayingRoot`、3画面ぶんの `Entity` と UI ツリー(上段=焼成室、下段左右=外・売り場の
      2x2 グリッド)の構築(`setup`)・破棄(`teardown`)。**画面外周の余白は付けない**——`root`
      の `padding` はベゼル程度(`px(6)`)のみで、`content`/`grid`/各画面ボックス(`kiln`/`bottom_row`)
      はすべて `flex_grow` でウィンドウの余り高さを奪い合う(比率は `Pane::capacity()` から直接取る
      8:5)。ウィンドウを伸ばしても増えるのは3画面それぞれの箱(`MONITOR_BG` で塗られた領域)であって、
      箱の外側の空白ではない——`render::container_height_px` が決める「行数ぶんのログ窓」の高さは
      これまでどおり固定のまま変えていない(第3.1節の許容行数はウィンドウサイズに左右されない)。
      ただし箱自体は伸びるので、各画面ボックスの `Node::justify_content` は `SpaceBetween` にしてある
      ——ヘッダを箱の上端、ログ窓を箱の下端に固定し、伸びた分の空白はヘッダとログ窓の「間」だけに
      入るようにするため。ここを `FlexStart`/`Center` 等に戻すと、ログ窓が箱の中央付近に浮いて
      見える形にデグレするので注意。`spawn_pane` の `node.padding`/`node.justify_content` の代入を
      消さないこと。`split_pane_node`(`Outside`/`Floor` 用)・`kiln` の `Node`・`spawn_pane` 内の
      `container` はいずれも `render::shrinkable` を通す(2026-07-20)——`flex_basis: 0` で等分を
      意図しても、`Node::min_width` の既定値(`Val::Auto`)は子の折り返し不可なコンテンツ幅を
      そのまま自分の最小幅として持ち上げてしまうため、噂話のように長い1行(`TextLayout::no_wrap()`)
      が来ると `Floor` だけ想定より広がり、グリッド全体の8:5比が崩れていた。`shrinkable` が
      `min_width: Val::Px(0.0)` を強制することで、どのパネルも自分の中身の長さでレイアウトを
      壊せなくなる。`spawn_pane` のパネル見出し(`header`)は、焼成室(`Pane::Kiln`)のときだけ
      `CURSOR_MARK`、それ以外は `CURSOR_BLANK` を先頭に付けて1回だけ組み立てる静的な `Text`——
      H/L で切り替わる選択状態ではなく、「操作対象はここだけ」を示す固定の目印(2026-07-21)。
      画面下部の凡例(`legend`)からも `"画面切替 H/L"` の項目を削除した
    - `pending.rs`: **カーソルと許容行数**。`Pending` は `Resource` ではなく各画面の `Entity` に載る
      `Component`(3画面ぶん存在する)。直近 `capacity`(画面ごとに異なる。`Pane::capacity` 参照)行の
      ウィンドウとカーソル位置を保持し、新しい行が来るとカーソルは既定で最新行に追従(`following`)するが、
      `J`/`K` で手動移動すると追従が外れる。`Entity::PLACEHOLDER` を使えば `App` なしで `push`/
      `move_cursor`/`mark_current` を直接単体テストできる(実装済み)
    - `render.rs`: **1行=1セル列(2026-07-21改稿)。** かつては1行=1つの `Text` エンティティ
      (整形済み文字列を毎フレーム丸ごと書き換え)だったが、いま各行は横並びの `Node` で、カーソル欄・
      検印欄・本文の文字1つ1つがそれぞれ独立した `Text`+`Node`+`BackgroundColor`+`BorderColor`を
      持つセル(`LineCells`、1行につき `cursor`・`mark`・`chars: Vec<Entity>`)になっている。`TextSpan`
      でも1文字ごとに色や書体は変えられるが、`TextSpan` はUIノードではないため塗りも枠線も持てない
      ——1文字ごとの背景・枠線を独立に制御できる必要がある `glitch.rs` の反転・縁取り演出のために、
      この粒度まで分解した。`sync_log_display` は3画面ぶんの `Pending` を1つのクエリで回して各セルの
      `Text` を `body_glyphs`/`mark_glyph` から再構築する(画面ごとに複製しない)。文字数ぶんの
      エンティティを毎フレーム触るコストを抑えるため、`set_cell_text` は実際に内容が変わるセルだけ
      書き込む(`Text` の `DerefMut` は内容が同じでも変更検知を発火させ、Parley側の再シェイプを
      引き起こすため)。表示欄の高さは `container_height_px(capacity)` で行数から直接算出し
      (`flex_grow` は使わない)、ウィンドウを伸縮しても見える行数は変わらない。パネル見出しの
      `CURSOR_MARK`(焼成室にだけ常時点灯)はもう毎フレーム更新するものではなく、`setup.rs` が
      スポーン時に一度だけ焼き込む静的な文字になった(2026-07-21、`sync_pane_headers`/
      `PaneHeader` ごと削除——下記参照)。**削除の
      見た目**は罰マークではなく、`═`(二重線の箱罫)を左のセルから順に上書きしていくアニメーション
      (`body_glyphs`)。取り消し線の合成文字(combining mark)はこのフォント/シェーパでは豆腐化するため
      使わない。`body_glyphs`/`mark_glyph` は純粋関数として単体テストあり。`render_resolved_line` は
      行が `Pending` から外れる瞬間に1回だけセルへ最終状態を書き込む(以後 `sync_log_display` はもう
      触らない)。`shrinkable(node) -> Node`(2026-07-20)は `min_width: Val::Px(0.0)` を強制するだけの
      小さな関数だが、`setup.rs` の各パネル箱・ログ窓と `spawn.rs` の行1つ1つが共通してこれを通る
      ことで、「長い1行がパネル幅を壊さない」という不変条件を1箇所の定義だけで担保している
      (詳細は `setup.rs` の項)
    - `spawn.rs`: `line_spawn` が3画面ぶんの `(PaneRuntime, LogUi, Pending)` を1つのクエリで回し、
      画面ごとに自分の `spawn_timer` で `domain::generate(pane, ...)` を呼ぶ。**遅延判定**は従来どおり:
      `削除`/`検印` を押しても即座には確定せず、対象行に `mark` が付くだけで、実際の `resolve()` 呼び出しと
      結果反映は、その行がウィンドウの前端から追い出される(=画面外に出る)瞬間に行われる
      (`Pending::push` の戻り値 `evicted` を処理)。`spawn_line_ui`(2026-07-21改稿)は行の文字数ぶん
      `spawn_cell` を呼んで `LineCells` を組み立て、横方向 `FlexDirection::Row` の `Node`(既定で
      `FlexWrap::NoWrap`)へ子として並べる——`Outside`/`Floor` は幅が狭いため、折り返すと `Pending` の
      「1行=1件」の前提が画面上の見た目と食い違ってしまうので、かつての `TextLayout::no_wrap()` と
      同じ効果をフレックスの折り返し無効で担う。行の `Node` は `render::shrinkable` を通す。
      `MAX_SPAWNED`(履歴として残す行数の上限、画面ごと)は1行=1エンティティだった頃の `400` から
      `60` に下げてある——いまや1行は文字数ぶんのエンティティ(`LineCells`)を抱えるため、同じ数値の
      ままだと解決済み(もう `Pending` の外で誰も触らない、画面外にクリップ済み)行のためだけに
      エンティティ数が数十倍に膨らんでしまう。表示上は一切影響しない(この上限を超えた古い行は
      そもそも画面に映らない)、純粋なメモリ上限の調整。`spawn_line_ui` は本文の各文字セルに
      `TEXTURE_DIM_CHANCE`(暫定 `0.1`)の確率でFGの代わりにDIMを焼き込む——行のスポーン時に
      1回引く乱数だけで決まり、以後アニメーションしない静的な「インクのムラ」(第8節例外3)
    - `input.rs`: `handle_line_input` は常に焼成室の `Pending` だけを探して
      `J`/`K`/`Z`/`X` を適用する(第3.2節: 操作できるのは焼成室だけで、画面を切り替えるという
      概念自体がない)。`animate_delete_wipe` は3画面すべてを毎フレーム進める——目を離した
      画面のワイプも止まらない、という点はそれでも変わらない
    - `intrusion.rs`: **呼ばれる**専用。`IntrusionSlot` は3画面のどの `Node` 木にも属さない独立した
      兄弟エンティティで、`Pending` を一切持たない——「不可触」を実装として保証している(禁止マナーとして
      注意喚起するのではなく、そもそも印をつける仕組みが存在しない)。`GameData::pending_intrusion` を
      拾って表示し、表示中に `Z`/`X` が押されれば「応えた」、`INTRUSION_LIFETIME_SECONDS` 経過まで無操作
      なら「静観」として、どちらも `domain::resolve(ShouldNotReact, ...)` で判定する
    - `pause.rs`: **一時停止**。`AppState` の遷移ではなく `PauseState` リソース + `PlayingRoot` の
      `Visibility` 切替で実装(状態遷移だとログ全体が破棄・再構築されてしまうため)。`ESC` で開閉、
      `J`/`K`/`Enter` で「続ける」/「タイトルに戻る」を選択
    - `glitch.rs`(2026-07-21拡張、同日中に縮小): **CRT質感、単発のみ**。`GlitchTimer` が
      5〜15秒おきに1回(暫定値)、3画面に表示中の全セル(`LineCells::chars`)からランダムに
      1文字を選び、`Substitute`(半角カタカナ等のノイズ字形へ一瞬だけ差し替え。`render::
      sync_log_display` が毎フレーム無条件に正しい表示を再構築するため次のフレームで自動的に
      治る)・`Invert`(FG/BGを入れ替えた反転表示を`EFFECT_DURATION_SECONDS`秒維持)・`Frame`
      (1pxの縁取りを同じ秒数維持)のいずれかを適用する。`Invert`/`Frame` は `Text` を
      書き換えないため `sync_log_display` では治らず、`ActiveGlitch` リソースが自前のタイマーで
      明示的に元へ戻す。特定の行の意味とは無関係に、画面全体からランダムに選んだ1文字だけへの
      短時間の作用(第8節例外1)。当初は加えて、常時アニメーションする「アンビエントシマー」
      (`ambient_shimmer`)も実装したが、全画面・全行が同じ波形で同時に切り替わるせいで
      「統一感がありすぎる」「チカチカする」ものになってしまったため撤回し、`spawn.rs` 側の
      静的な焼き込み(第8節例外3、上記)に置き換えた——常時系の質感は二度と時間で動かさない、
      という判断はこの失敗を踏まえたもの
    - `day_indicator.rs`(2026-07-20追加): **日数インジケータ**。焼成室ログ上段の左側に固定幅
      (`INDICATOR_WIDTH`、`flex_grow`は使わない——焼成室ログ側の`flex_grow: 1.0`が余った幅を
      すべて引き受ける)で差し込む、4桁の7セグメント風表示。各桁は7つの矩形(`Segment`列挙・
      `DIGIT_SEGMENTS`の標準7セグメントエンコード表)を`BackgroundColor`のFG/DIM切替だけで
      点灯/消灯させる——文字ではなく矩形そのものを使う点で第8節の「文字のみ」原則の狭い例外
      (詳細は第8節の例外2)。最初はダミー「8」の文字を薄く敷いてその上に本物の数字を重ねる
      実装を試したが、2枚の`Text`のグリフ輪郭がセグメント単位で一致せず、ただの滲みにしか
      見えなかったため、本物のセグメントごとの矩形制御に作り直した。`GameData::day`から
      毎フレーム再導出する(`sync_day_indicator`、`render::sync_log_display`と同じ設計)
    - `corruption.rs`: 侵食度(3画面共有の単一値)が100に達すると `AppState::Lost` へ
- `src/fonts.rs`: `src/assets/fonts/` の3書体を起動時に読み込む。通常行・脅威行・呼びかけ行はすべて
  Noto Sans JP で統一し(第2原則・第4節「異常は筆癖でバレる」を書体差で破らないため)、第7節の演出の
  到達点だけ書体を切り替える:二人称の障りは Potta One、呼ばれる/Lost画面の侵食ログは Yuji Syuku

**未実装/簡略化した箇所(第9節と対応):**
- 脅威図鑑・禁忌集は本実装が定めた3種の主異常(表記の乱れ・数え違い・記載漏れ/反復/呼びかけ)と数点の
  メモのみのプレースホルダー。全項目リストは今後の検討のまま
- 経済(`income`)は内部で加減算しているが、UIには一切出さず(第8節の「目標表示禁止」に準拠)、侵食度への直接的なフィードバックも未実装。売上と侵食率のバランス調整は今後の課題
- グラフィック/罫線/色による強調は使わず、`src/theme.rs` の単色(FG/DIM)のみで表現。例外は第8節に明記の
  CRTグリッチと日数インジケータの2つのみ。
  `MONITOR_BG` は `BG` からの差分が sRGB 換算で数値ぶんしかないと8bit量子化で丸まって画面上は無色に見えるため、
  知覚できる程度の差を保つこと(強調ではなく「モニタごとの塗り」自体が見えなくなるのは本末転倒)
- 3画面の「主な異常」への重み付け(`domain::generate::weights_for`)は暫定値。バランス調整は今後の課題(第9節)

## 開発フロー

コード変更のたびに以下を実行し、整形・lintを通した状態を保つ([rustfmt.toml](rustfmt.toml) に設定あり)。

```sh
cargo fmt
cargo clippy --fix --allow-dirty
```

## 対象プラットフォーム

Windows / Linux / macOS のみをサポートする。Android / iOS / Wasm などモバイル・Web ターゲットは対応しない。

- ツールチェーンは nightly を使用する（[rust-toolchain.toml](rust-toolchain.toml)）。
- プラットフォーム別ビルドは [.cargo/config.toml](.cargo/config.toml) の `cargo build-windows` / `cargo build-linux` / `cargo build-macos` エイリアスを使う。

## ビルド最適化

[Bevy Quick Start: Compile with Performance Optimizations](https://bevy.org/learn/quick-start/getting-started/setup/#compile-with-performance-optimizations) に準拠。

- `[profile.dev]` / `[profile.dev.package."*"]`: dev ビルドの実行速度を確保しつつ、依存クレートのみ最適化してコンパイル時間を抑える。
- `[profile.release]`: `codegen-units = 1` と `lto = "thin"` でリリースバイナリの実行性能を優先。
- `log` クレートに `max_level_debug` / `release_max_level_warn` を指定し、リリースビルドで不要なトレースログを実行時にフィルタしない（コンパイル時に除去）。
- 開発時は `cargo run --features dev` で `bevy/dynamic_linking` を有効化し、リンク時間を短縮する。出荷用ビルドでは使わない。
- リンカ: Windows は `rust-lld.exe`（`cargo install -f cargo-binutils` と `rustup component add llvm-tools-preview` が必要）、Linux は `clang` + `lld`（`lld` と `clang` のインストールが必要）。macOS は標準リンカ（ld-prime）がすでに高速なため未設定。
- Linux ターゲットには nightly 限定の `-Zshare-generics=y` を設定しジェネリックコードの重複コンパイルを削減。
